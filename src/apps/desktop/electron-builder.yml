    appId: com.buuhvprojects.anidock-hub
    productName: AniDock Hub
    directories:
      output: dist
      buildResources: resources
    files:
      - filter:
          - ./package.json
          - ./dist-electron/**
          - ./out/renderer/**
    win:
      target:
        - target: nsis
          arch:
            - x64
      icon: resources/icon.ico